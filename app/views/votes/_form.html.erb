<%= form_for(@vote) do |form| %>
  <% if @vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
        <% @vote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, value: current_user.id %>
  </div>

  <div class="field">
    <%= form.label :contribution_id %>
    <%= form.number_field :contribution_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
